#Properties for sb-login and sb-counters
# En un solo fichero por simpl
# Login config
# Hibernate
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# Esto es para que cree las tablas automáticamente (creo)
spring.jpa.hibernate.ddl-auto = update
logging.level.edu.uv.twcam.login=DEBUG
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.datasource.username=root
spring.datasource.password=superSecretPW
spring.datasource.url=jdbc:mysql://mysql:3306/users?useSSL=false&allowPublicKeyRetrieval=true
expiration.token.time=3600000
secret.token=secretSwarmSalt

# Counters Config
REDIS_COUNTERS_SERVICE_HOST=localhost
REDIS_COUNTERS_SERVICE_PORT=6379
SERVER_PORT = 8081